name: S3-synchronization-cronjob

on:
  push:
    branches:
      - jamesA-githubactions-testing

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: jamesA-githubactions-testing # Change to master later after review
          token: ${{ secrets.GH_TOKEN }}
      - name: Generate random number
        id: random
        run: echo "::set-output name=value::$(echo $RANDOM)"
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"
      - name: Create branch
        uses: peterjgrainger/action-create-branch@v2.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          branch: sync-${{ steps.random.outputs.value }}
      - uses: actions/checkout@v4
        with:
          ref: sync-${{ steps.random.outputs.value }}
          token: ${{ secrets.GH_TOKEN }}
      - name: create env file
        run: |
          touch .env
          echo AWS_STARPORT_BAR_AK=${{ secrets.AWS_STARPORT_BAR_AK }} >> .env
          echo AWS_STARPORT_BAR_SAK=${{ secrets.AWS_STARPORT_BAR_SAK }} >> .env
          echo REPOSITORY_ID=${{ secrets.REPOSITORY_ID }} >> .env
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
      - run: npm install
      - run: npm run sync-script
      - name: commit and push new file
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "<>"
          git add .
          git commit --allow-empty -m "Sync docs folder"
          git push
      # - name: create pull request
      #   uses: peter-evans/create-pull-request@v5
      #   with:
      #     token: ${{ secrets.GH_TOKEN }}
      #     commit-message: Auto Pull Request
      #     title: Docs auto-synchronization with S3
      #     body: Auto-created Pull Request
      #     branch: sync-${{ steps.random.outputs.value }}
      #     base: jamesA-githubactions-testing # Change to master later after review
